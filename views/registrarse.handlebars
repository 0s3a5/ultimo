<!DOCTYPE html>
<html lang="es">
<head>
  <link href="/login/fondologin.css" rel="stylesheet" type="text/css" />
  <link href="/login/login.css" rel="stylesheet" type="text/css" />
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bootstrap demo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body class="fondologin">
  <div class="login-box">
    <p>Registrarse</p>

    {{#if error}}
      <p style="color: red;">{{error}}</p>
    {{/if}}
    
    <form action="/usuarios" method="POST" oninput="checkPasswords()">
      
      <div class="user-box">
        <input required name="email" type="email" pattern="[a-zA-Z0-9._%+-]+@(gmail|email)(\.udp)?\.(com|cl)" title="Por favor, introduce un correo válido (gmail.com, gmail.cl, gmail.udp.com, gmail.udp.cl, email.com, email.cl, email.udp.com o email.udp.cl).">
        <label>email</label>
      </div>

      <div class="user-box">
        <input required id="password" name="password" type="password">
        <label>Contraseña</label>
      </div>
      <div class="user-box">
        <input required id="confirm-password" name="confirm-password" type="password">
        <label>Confirmar Contraseña</label>
      </div>

       <div class="user-box">
        <input required id="rango" name="rango" type="rango">
        <label>rango(1:user 2:admin)</label>
      </div>
    <div style="height: 3vh;"></div>
      <div>
        <button class="btn" id="submit-button" type="submit"><i class="animation"></i> Iniciar <i class="animation"></i></button>
      </div>
      <p id="error-message" style="color: red; display: none;">Las contraseñas no coinciden.</p>
    </form>

    <p class="text-white fs-6">¿Ya tienes una cuenta? <a href="/login" class="text-white fs-6">¡Iniciar sesión!</a></p>
  </div>

  <script>
    function checkPasswords() {
      const password = document.getElementById('password').value;
      const confirmPassword = document.getElementById('confirm-password').value;
      const submitButton = document.getElementById('submit-button');
      const errorMessage = document.getElementById('error-message');

      if (password === confirmPassword && password !== '') {
        submitButton.disabled = false;
        errorMessage.style.display = 'none';
      } else {
        submitButton.disabled = true;
        errorMessage.style.display = 'block';
      }
    }
  </script>
</body>
</html>
